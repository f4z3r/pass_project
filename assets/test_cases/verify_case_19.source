x := source();
y := source();
z := source();
s := source();
if(t > 0) {
    u := x + y;
    v := y + z;
    w := z + s;
}
else {
    u := y + z;
    v := z + s;
    w := x + y;
}
sink(u);
sink(v);
sink(w);
sink(x);
sink(y);
sink(z);
sink(s);


//expected output:  ("l5","x"),("l5","y"),("l5","z"),("l5","s").

